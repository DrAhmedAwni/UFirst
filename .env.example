# Keep false for the public-only deployment. Set true after connecting D1.
UFIRST_ADMIN_ENABLED=false

# Comma-separated email addresses that may access /admin.
# Configure this in the hosted site's environment before publishing.
UFIRST_ADMIN_EMAILS=your-email@example.com
